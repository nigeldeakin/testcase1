build:
  box: golang
  docker: true
  steps:
    - script:
        name: A script step
        code: |
          export FOO=BAR
          export VAR1=$FOO
          export VAR2='$FOO'
          export VAR3="$FOO"
    - script:
        name: Abother script step
        code: |
          export VAR1=$FOO
          export VAR2='$FOO'
          export VAR3="$FOO"

